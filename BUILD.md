# roc_desk-http standalone build

Run from the repository root:

`powershell
.\build-standalone.ps1
`

The release executable is written to in\roc_desk-http.exe. Use
-Configuration debug for a fast development build.
